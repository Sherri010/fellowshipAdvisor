<div class="intro">
<!-- <img id="banner" src="http://i.imgur.com/dDznL1V.png"> -->
</div>
<div class="cities-wrap">

    <div class="row">

  <% @cities.each do |city| %>

<style>

  .<%=city.name.delete("  '")%> {
    background:linear-gradient(
        rgba(0, 0, 0, 0.5),
        rgba(0, 0, 0, 0.5)), url(<%=city.picture%>);
    cursor: pointer;
  }
  </style>



    <!-- <div class="col-xs-3 col-xs-6 col-offset-2 col-md-9"> -->
      <a href='<%=city_path(city.id)%>' class=" city-image thumbnail <%=city.name.delete(" '")%>">
        <p class="city-title-overlay"><%= city.name %></p>
      </a>


  <% end %>
</div>
</div>
